production:
  cleanup_old_records:
    class: ScheduledCleanupJob
    queue: low_priority
    schedule: every hour

development:
  cleanup_old_records:
    class: ScheduledCleanupJob
    queue: low_priority
    schedule: every hour
  warm_homepage_cache:
    class: WarmCacheJob
    queue: low_priority
    schedule: every 15 minutes
